# Demo Script
1) Login/Register → show password rules and lockout behavior.
2) Dashboard → view CPU/memory/latency/error rates; note mock data when Prometheus is down.
3) Live updates → connect to /ws/updates, unplug network to show HTTP polling fallback.
4) Alerts → trigger threshold to show Slack/email readiness (describe or simulate).
5) Admin users view → show role-gated area.
